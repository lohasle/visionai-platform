# VisionAI 1.0 发布说明

首次完整交付，覆盖设计文档定义的数据、标注、数据集、训练、评估、模型、部署、反馈、资源、集成、审计和交付能力。

升级前必须执行 `scripts/backup.*`。应用升级使用 `docker compose up -d --build --wait`；数据保留在命名卷中。若升级失败，恢复旧镜像后按产品手册执行数据库与对象存储恢复。

生产部署必须覆盖默认密码和 JWT Secret，限制基础设施管理端口的网络访问，并为对象存储、数据库、CVAT 和 FiftyOne 配置周期性快照。
