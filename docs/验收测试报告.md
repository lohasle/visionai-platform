# VisionAI 验收测试报告

执行日期：2026-07-24
环境：Windows 11 / Docker Desktop Linux Containers / MySQL 8.4 / CVAT 2.61.0 / FiftyOne 1.15.0
结论：核心黄金路径和交付门禁通过。

## 自动化门禁

| 检查 | 结果 |
| --- | --- |
| `go test ./...` | 通过 |
| `pnpm ts:check` | 通过 |
| `pnpm build:prod` | 通过 |
| `docker compose config --quiet` | 通过 |
| Windows/Linux/GPU override 解析 | 通过 |
| 路由契约测试 | 通过 |
| ClearML Provider 客户端契约测试 | 通过 |
| 推理 Provider 客户端测试 | 通过 |
| FiftyOne Provider 客户端测试 | 通过 |
| CVAT iframe 安全响应头回归检查 | 通过 |
| 空库初始化（94 张表）不创建会员/支付表或菜单 | 通过 |

## 端到端黄金路径

| 阶段 | 真实运行证据 | 结果 |
| --- | --- | --- |
| 数据进入 | 从 Ultralytics 官方发布下载并导入 128 张 COCO128 真实图片；导入 128/128 成功 | 通过 |
| 集合 | 创建 128 资产集合并冻结 | 通过 |
| 标注 | 创建 CVAT Task，导入 929 个真实对象框，复核、导出并关闭 | 通过 |
| 数据集 | 生成不可变 v6，按 104/16/8 拆分并冻结；0 阻断、2 个合法空标注警告 | 通过 |
| 训练 | LocalDocker 暂存 128 张真实图片，以 `--gpus 1` 在 RTX 3060 执行 12 Epoch CUDA 计算，保存权重、指标、环境锁和日志 | 通过 |
| 评估 | 向 FiftyOne 同步 128 样本、GT、预测、IoU 和错误类型；mAP 0.723，门禁通过 | 通过 |
| 内嵌工作台 | VisionAI 内嵌 CVAT/FiftyOne，保留业务上下文与外部打开兜底 | 通过 |
| 回归门禁 | 与基线比较，mAP 差异 0，最大允许回归 0.02 | 通过 |
| 模型 | 注册模型版本、Model Card、制品哈希和供应链证据 | 通过 |
| 四眼审批 | 管理员自审返回 409；独立 reviewer 审批成功 | 通过 |
| 部署 | 生产部署成功，3 次图片推理返回 Trace/模型/修订；P95/P99 10.03 ms | 通过 |
| 视频推理 | 真实解码 320×240 MP4 的 5 帧，逐帧返回检测、时间戳与 Trace | 通过 |
| 回滚 | 从修订 1 创建不可变修订 2 并切换 | 通过 |
| 反馈 | 低置信度样本捕获、SHA 去重、Batch 回流 CVAT | 通过 |
| 资源与集成 | 自动发现 RTX 3060（12 GiB）、`gpu-local` 队列、配额、CVAT 内部健康端点 | 通过 |
| 审计 | 关键操作生成并可查询/导出审计事件 | 通过 |

## 跨平台用例

| 编号 | 用例 | 结果 |
| --- | --- | --- |
| WIN-E2E-001 | Docker Desktop 环境诊断与 Compose 解析 | 通过 |
| WIN-E2E-002 | 数据资产上传/导入与 MinIO 访问 | 通过 |
| WIN-E2E-003 | CVAT 标注往返 | 通过 |
| WIN-E2E-004 | LocalDocker 训练与制品回收 | 通过 |
| WIN-E2E-005 | FiftyOne 评估工作台 | 通过 |
| WIN-E2E-006 | 部署、推理、回滚与反馈闭环 | 通过 |
| WIN-E2E-007 | CVAT/FiftyOne iframe 内嵌、刷新、返回与外部兜底 | 通过 |
| LINUX-E2E-001 | Linux override 静态解析和共享 amd64 镜像构建 | 通过 |
| CROSS-E2E-001 | Manifest 不含 Windows/WSL 绝对路径 | 通过 |
| CROSS-E2E-002 | Model Card/参数快照只含逻辑 URI 和哈希 | 通过 |
| CROSS-E2E-003 | 浏览器、容器内 MinIO 双端点预签名 | 通过 |

最终交付栈使用 `docker compose up -d --build --wait` 构建并启动，全部 22 个常驻服务达到 running/healthy，CVAT Bootstrap 以退出码 0 完成。容器化 Web 登录、Nginx API 反向代理、MinIO 浏览器预签名（HTTP 200）、Orchestrator 共享卷训练制品写回和视频推理均在交付镜像上复验通过。

公开数据验收可通过 `scripts/e2e-coco128.ps1` 重放。数据源为 [Ultralytics COCO128](https://docs.ultralytics.com/datasets/detect/coco128/)，本次归档 SHA-256 为 `61E5E3028863D8FFC3B81D6A514603954889F0EDD5E4B44C4CE60B2DA99AEB8E`。

RTX 3060 复验新建项目 #14：DatasetVersion #9 `FROZEN`、TrainingRun #6 `SUCCEEDED`（`gpuCount=1`、`imageCount=128`、CUDA Runtime 12090）、EvaluationRun #9 `PASSED`、Deployment #5 `RUNNING`、Feedback AnnotationTask #15 `READY`。图片回归使用真实 JPEG multipart 上传并通过，机器可读结果保存在 `docs/evidence/loh-20-rtx3060-coco128.json`。

Linux 用例在 CI 的 Ubuntu Runner 上执行编译、前端构建和 Compose 配置验证；完整外部 CVAT/FiftyOne 运行验收在本地 Docker Linux 容器环境执行。

## SPEC-2240 系统角色统一验收

- 角色定义与用户角色分配统一复用系统 `roles` / `user_roles`，项目仅维护成员范围。
- 项目成员页面不再编辑角色，实时展示系统角色名称。
- 旧项目角色 JSON 已自动迁移；移除审核员系统角色后受保护 API 即时返回 403，恢复后重新可用。
- COCO128 新建项目 #16 全链路通过：GPU 训练 #8 SUCCEEDED、评估 #11 PASSED、部署 #7 RUNNING、反馈任务 #19 READY。
- 证据：`docs/evidence/loh-21-system-role-e2e.json`。

## 截图证据

- `project-center.png`：项目中心
- `asset-center.png`、`asset-import-dialog.png`：数据资产与导入
- `dataset-registry.png`、`dataset-quality-report.png`：数据集版本与质量
- `training-experiment.jpg`：训练、指标与制品
- `evaluation-gate.jpg`、`evaluation-hard-samples.jpg`：评估门禁与困难样本
- `cvat-embedded-workbench.png`、`cvat-annotation-editor.png`：VisionAI 内嵌 CVAT 与 COCO128 原生标注编辑器
- `fiftyone-embedded-workbench.png`：VisionAI 内嵌 FiftyOne 128 样本数据集
- `model-registry-approval.jpg`：模型注册和四眼审批
- `deployment-monitoring.jpg`：部署修订、指标和告警
- `feedback-loop.jpg`：生产反馈闭环
- `resources-integrations.jpg`：资源、集成与审计

## 已知非阻断项

- 演示推理 Provider 输出确定性的检测结果，用于验证平台协议、视频解码、监控和反馈链路；生产模型镜像应实现同一 Provider 契约。
- GPU Compose 需要主机预先安装 NVIDIA Container Toolkit；纯 CPU 默认栈无需 GPU。
- 完整栈首次拉取 CVAT/FiftyOne 镜像耗时受网络带宽影响。
